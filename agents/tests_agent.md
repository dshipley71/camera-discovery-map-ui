# Tests Agent

Owns deterministic unit tests and verification commands.

Rules:

- Tests must not require network access.
- Fixtures must be clearly local/minimal and must not pretend to be real cameras.
- Do not weaken assertions to hide incomplete behavior.
- Protect artifact, status, media, CLI, and template contracts.
